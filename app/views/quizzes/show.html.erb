<% content_for :title, @quiz.title %>

<%= render @quiz %>

<div>
  <%= link_to "Edit this quiz", edit_quiz_path(@quiz) %> |
  <%= link_to "Back to quizzes", quizzes_path %>

  <p>
    <%= button_to "Delete this quiz!", @quiz, method: :delete %>
  </p>
</div>
